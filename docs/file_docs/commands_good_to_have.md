# Basic commands that are good to have
A few basic commands that are good to have that will be filled more and more.


- Rensa Silver-data + checkpoint
    - ` rm -rf data/silver/* ` Tar bort silver data foldern
    - ` rm -f data/checkpoints/*.json ` Tar bort checkpoint FILEN    


- Bygg om flera docker sessions samtidigt
    - ` docker compose build spark dbt quality `
    