apiVersion: 1
datasources:
  - name: DuckDB – GitHub Lake
    type: motherduck-duckdb-datasource    # <--- ÄNDRAT: nytt namn
    uid: github-lake-duckdb               # <--- Fast UID som dashboard JSON kan referrera till
    access: proxy
    isDefault: true
    jsonData:
      path: /app/data/dbt/github_lake.duckdb